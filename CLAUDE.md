# CLAUDE.md

`travel-poster-system` 的项目级约定。本文件的规则覆盖全局默认行为。

## Git 工作流

**直接在 `main` 上提交，不要开分支。**

全局默认是「在默认分支上先开分支再提交」—— 本仓库不适用：单人开发，没有评审
流程要配合，分支只是多出合并与清理两步。

- 用户说「提交」时，直接 `git commit` 到 `main`，不建 feature 分支、不建 PR
- 推送仍需用户明确要求（这一条不变）
