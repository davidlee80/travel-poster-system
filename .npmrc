# 拒绝在不符合 engines 的 Node/pnpm 上安装，避免"本地能跑、CI 不能跑"
engine-strict=true

# workspace 内部依赖只允许用 workspace: 协议解析，防止误从 registry 拉同名包
link-workspace-packages=deep
prefer-workspace-packages=true

# 锁文件必须提交且在 CI 中生效
lockfile=true
