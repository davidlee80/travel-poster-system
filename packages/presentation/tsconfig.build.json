{
  // 构建配置：排除测试文件，避免 *.test.js 进入 dist 与生产镜像。
  // tsconfig.json 本身包含测试，供 typecheck 与 ESLint projectService 使用 ——
  // 测试代码同样需要类型检查。
  "extends": "./tsconfig.json",
  "exclude": ["src/**/*.test.ts", "src/**/__tests__/**"]
}
